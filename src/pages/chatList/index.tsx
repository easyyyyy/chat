import React, { useState, useEffect } from 'react'
import {
  Form,
  Input,
  Button,
} from 'antd-mobile'
import cssExports from './index.scss'
import { login } from '@api/index'

const ChatList: React.FC<any> = (props: any) => {

  return (
    <div>
      <div>chatlist</div>
    </div>
  )
}
export default ChatList
